version: "1.0"
linter: jetbrains/qodana-python-community:2024.1

bootstrap: |
  cd ./backend
  poetry install

profile:
  name: qodana.recommended
include:
  - name: CheckDependencyLicenses
exclude:
  - name: All
    paths:
      - backend/migrations
      - backend/auth/tests
